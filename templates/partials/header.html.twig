
{% block body %}
<nav class="navbar navbar-dark navbar-expand-lg fixed-top bg-dark" id="mainNav" style="background: radial-gradient(rgb(255,255,255) 100%, white 100%, rgb(246,246,246) 100%), linear-gradient(black, white 0%), rgb(255,255,255);color: rgb(255,255,255);border-color: rgb(0,0,0);border-left-color: rgb(255,255,255);">
    <div class="container">
        <a class="navbar-brand" href="{{ path('home') }}"  style="color: var(--bs-green);">Coworking Space</a>
        <button data-bs-toggle="collapse" data-bs-target="#navbarResponsive" class="navbar-toggler navbar-toggler-right" type="button" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            <i class="fa fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ms-auto text-uppercase">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('home') }}" style="color: rgb(0,92,15);">ACCUEIL</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('Reservation') }}" style="color: rgb(0,92,15);">SERVICES</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('about') }}" style="color: rgb(0,92,15);">A PROPOS</a>
                </li>

            {% if app.user %}
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink" style="color: rgb(0,92,15);">
                        <img src="{{ vich_uploader_asset(app.user) }}"alt="." class="avatar avatar-mini">{{app.user.fullName}}
                    </a>

                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                        <a href={{ path('Reservation')}} class="dropdown-item" style="color: rgb(0,92,15);">Réserver</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('account_index') }}" class="dropdown-item" style="color: rgb(0,92,15);">Mon compte et mes réservations</a>
                        <a href="{{ path('account_profile') }}" class="dropdown-item" style="color: rgb(0,92,15);">Modifier mon profil</a>
                        <div class="dropdown-divider"></div>
                        <a href="/logout" class="dropdown-item" style="color: rgb(0,92,15);">Déconnexion</a>
                    </div>
                </li>
            {% else %}

                <li class="nav-item"><a class="nav-link" href="{{ path('app_login') }}" style="color: var(--bs-green);">CONNEXION</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('app_register') }}" style="color: var(--bs-green);">INSCRIPTION</a></li>
            {% endif %}
             </ul>
    </div>
    </div>
</nav>
{% endblock %}